import styled from 'styled-components';

export const Section = styled.section`
  h1 {
    font-size: 40px;
    display: flex;
    align-items: center;
    gap: 16px;
  }

  .cronometro {
    color: var(--primary-color);
  }
`;
